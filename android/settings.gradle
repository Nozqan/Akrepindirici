pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'akrepindirici'
include ':app'
project(':app').projectDir = new File(rootProject.projectDir, 'app')
